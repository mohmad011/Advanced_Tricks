/* global $, smoothscroll*/

$(function () {

	'use strict';

	// Make Div Height Same As Window Height

	$('div').height($(window).height());

	// Initialize Smooth smoothscroll
	// smoothScroll.init(); this will be speed == 500 and you can edite on it

	// smoothScroll.init({ this if you need edite on it 
 
	// 	speed:1000,
	// 	updataURL: false,
	// 	offset:200
	// });

});